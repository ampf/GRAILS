The @MpfDetector@ Grails plugin provides grails application (the "host app") with the ability to execute a media-processing analysis function (*detectors*), primarily those based on the  Appscio Media Processing Framework (*MPF*), to receive log in, status, and event messages from that function, and to persist the results of the analysis in domain objects. The @detector@ and domain design is extensible, with each @detector@ being
